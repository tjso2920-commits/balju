# 발주패스터

룸앤쿡 직원 18명이 현장에서 쓰는 음성인식 발주 앱.
사용 환경: 대부분 아이폰 + iOS Safari, 홈화면에 아이콘으로 추가해서 사용.
유지보수자는 1명이고 개발자가 아니다.

---

## 절대 규칙 (물어보지 말고 그냥 지켜)

1. **단일 HTML 파일 유지**
   vanilla HTML/CSS/JS + localStorage. CSS·JS를 별도 파일로 분리하지 말 것.
   빌드 도구, npm, 프레임워크 도입 금지. 개선 제안도 하지 말 것.

2. **파일명·경로 변경 금지**
   `index.html` 경로 고정. 파일명이 바뀌면 배포 주소가 바뀌고,
   직원 18명 폰 홈화면 아이콘이 전부 죽는다. 리네임·폴더 이동 제안 금지.

3. **localStorage 키 이름 변경 금지**
   - `baljuFaster_v2` — 발주 데이터 전체
   - `baljuFaster_gkey` — 사용자별 Gemini 키

   키를 바꾸면 직원들이 쌓아둔 데이터가 날아간다. 스키마를 바꿔야 한다면
   기존 키를 읽어 새 키로 옮기는 마이그레이션 코드를 같이 넣고, 먼저 승인을 받아라.

4. **iOS Safari 기준으로 판단**
   신규 웹 API를 쓰기 전에 iOS Safari 지원 여부를 먼저 확인.
   데스크톱 크롬에서 된다는 이유로 통과시키지 말 것.
   특히 음성인식(SpeechRecognition), 마이크 권한, 입력 포커스 동작 주의.

5. **터치 영역은 44×44px 이상**
   주방에서 장갑을 끼거나 젖은 손으로 쓰는 앱이다. 작은 버튼은 오작동한다.

---

## API 키 정책 (확정)

| 버전 | `GEMINI_API_KEY` 상수 | 사용자 키 입력 모달 |
|---|---|---|
| 내부용 (룸앤쿡 직원 배포) | **무료 등급 키 하드코딩** | 폴백으로만 동작 |
| 상용 / 크몽 판매용 | 빈 문자열 `""` | 활성 (코드에 이미 구현됨) |

### 내부용 배포 시 반드시 지킬 것

- 하드코딩하는 키는 **결제 수단이 연결되지 않은(Free Plan) Google 프로젝트의 키**여야 한다.
  정적 HTML 배포에서는 클라이언트의 키를 숨길 방법이 원리적으로 없다. 대신 노출되어도
  금전 피해가 0이 되도록 무료 등급 키를 쓴다. 최악의 경우는 일일 할당량 소진뿐이다.
- `Paid` 플랜 키를 절대 하드코딩하지 마라. 발견하면 즉시 경고하고 작업을 멈춰라.
- 확인처: https://aistudio.google.com/api-keys 의 Plan 컬럼
- 키 유출이 확인되면 대응은 단순하다 — 삭제하고 새로 발급해 재배포.

### 코드 동작 참고

`geminiKey()` 함수는 하드코딩 상수를 먼저 보고, 비어 있으면 localStorage를 폴백으로 읽는다.
따라서 상수에 키가 있으면 직원은 아무 설정도 하지 않아도 된다.

관련 상수:
- `GEMINI_MODEL = "gemini-flash-lite-latest"`
- `GEMINI_FALLBACK = "gemini-flash-latest"`

---

## 배포

- GitHub Pages. `main` 브랜치에 올리면 자동 재배포.
- 파일 경로가 유지되면 주소는 바뀌지 않는다 → 직원 홈화면 아이콘 유지.
- PWA manifest·서비스워커가 없어서 캐시는 일반 브라우저 캐시뿐이다.
  배포 후 폰에서 옛 버전이 보이면 사파리에서 주소를 직접 열고 새로고침.

---

## 구성

**매장 6곳**: 비원 · 하제 · 시오리 · 룸 · 한 · 한식

**발주처**
- 모노마트 — 텍스트 발주 (카톡 등으로 보낼 문장 생성)
- 비원발주 — 손글씨 발주판
- 사시미 발주 — 외부 수산물 도매 주문 페이지로 이동하는 타일 + 필독사항 모달.
  `SASHIMI_ORDER_URL` 상수. localStorage를 쓰지 않는다.

거래처를 추가·수정할 때 기존 발주 데이터 구조를 깨지 않게 할 것.

---

## 파일 구조 주의

**258행은 html2canvas 압축 번들이 인라인으로 들어있다** (약 196,000자 한 줄).
읽거나 수정하지 말 것. grep·검색 시 이 줄을 제외해야 출력이 깨지지 않는다.
앱 로직은 259행 이후의 `<script>` 블록에 있다.

---

## 커밋 전 체크리스트

- [ ] 단일 HTML 파일 상태가 유지됐는가
- [ ] `GEMINI_API_KEY`가 이 브랜치에 맞는 값인가 (내부용이면 Free 플랜 키가 들어있는가)
- [ ] localStorage 키 이름이 그대로인가
- [ ] 파일명이 `index.html`인가
- [ ] iOS Safari에서 동작하는 코드인가
- [ ] 새로 만든 터치 요소가 44×44px 이상인가

## 커밋 메시지

한국어, 기능 단위 한 줄.
예: `사시미 발주 타일 추가`, `필독 버튼 터치 영역 확대`

---

## 사용자 응대 방식

- git 용어에 익숙하지 않다. `pull`/`commit`/`push`를 영어 용어로 설명하지 말고
  "받기 / 저장 / 올리기" 수준으로 안내하고 명령은 직접 실행해라.
- 결론을 먼저 한 줄로 말하고, 그다음 최소한의 설명만 붙여라.
- 여러 안과 근거를 한 번에 길게 늘어놓으면 전달이 안 된다.
